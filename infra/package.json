{
  "name": "vectorvault-infra",
  "version": "0.1.0",
  "private": true,
  "description": "VectorVault shared-memory AWS infrastructure (CDK, TypeScript) — PR 1",
  "scripts": {
    "build": "tsc",
    "test": "ts-node test/verify-template.ts && ts-node test/verify-monitoring.ts",
    "synth": "cdk synth",
    "diff": "cdk diff",
    "deploy": "cdk deploy",
    "destroy": "cdk destroy"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "aws-cdk": "2.1129.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "aws-cdk-lib": "2.261.0",
    "constructs": "^10.4.2"
  }
}
